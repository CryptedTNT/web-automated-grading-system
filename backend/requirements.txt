fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pymysql==1.1.1
cryptography==44.0.0
werkzeug==3.1.3
python-dotenv==1.0.1
python-multipart==0.0.20
itsdangerous==2.2.0

# --- ML inference (YOLOv11 detector + TrOCR recognizer) ---
# Install torch/torchvision BEFORE this file, with the CUDA index URL,
# or `pip install -r requirements.txt` will pull in a CPU-only torch
# as a transitive dependency of ultralytics/transformers instead --
# see backend/SETUP.md.
ultralytics>=8.3.0
transformers>=5.0.0
safetensors>=0.4.5
pillow>=10.0.0
numpy>=1.26.0
